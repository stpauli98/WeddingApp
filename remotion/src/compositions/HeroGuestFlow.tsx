import { AbsoluteFill } from 'remotion';
import { theme } from '../theme';

export const HeroGuestFlow: React.FC = () => {
  return (
    <AbsoluteFill style={{ backgroundColor: theme.lpBg }}>
      {/* Placeholder — compositions built in Bundle 2 */}
    </AbsoluteFill>
  );
};
